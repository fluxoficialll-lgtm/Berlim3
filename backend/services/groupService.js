
import { GroupRepository } from '../repositories/GroupRepository.js';

class GroupService {
    // Exemplo de método
    /*
    async findGroupById(id) {
        // Lógica para buscar o grupo por ID
        return await GroupRepository.findById(id);
    }
    */
}

export const groupService = new GroupService();
